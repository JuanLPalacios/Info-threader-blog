<a href="<%= show_user_path(user) %>" class="contextual-link">

    <article class="user-card">
        <%= image_tag(user.photo, class: "user-image") %>
        <div>
            <h1>
                <%= user.name %>
            </h1>
            <span>
                Number of posts: <%= user.posts_counter %>
            </span>
        </div>
    </article>
</a>
